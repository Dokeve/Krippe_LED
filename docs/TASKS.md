# TASKS

1. Persistenz/Forms (Backend-Fehlerobjekte, Frontend-Autosave).  
2. Audio-Picker (Whitelist-Readdir, UI-Baum, Filter).  
3. LED-Zeitfenster (DB-Felder, Scheduler, UI).  
4. morgan-Logging (npm i morgan; app.use(morgan('combined'))).  
5. Systemd-Cleanup (nur nativity aktiv).
